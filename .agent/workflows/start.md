---
description: Boot the Athena system and load context
---

# /start — Execution Script

> **Latency Profile**: ULTRA-LOW (<2K tokens boot)  
> **Philosophy**: Boot fast. Load later.

## Phase 1: Instant Boot
- Load the current framework identity:
  - `.framework/v8.2-stable/modules/Core_Identity.md`
- Load the framework orientation:
  - `.framework/v8.2-stable/modules/Athena_Xero_Orientation.md`
- Load the current tag map:
  - `.context/TAG_INDEX.md`
- Load the current spiral tag map:
  - `.context/TAG_INDEX_SPIRAL.md`
- Load the map of interactional hygiene outline:
  - `.context/specs/sources/hygiene-map/hmap-master-outline.md`
- Load the map of interactional hygiene p001:
  - `.context/specs/sources/hygiene-map/hmap-p001.md`
- Load the map of interactional hygiene p002:
  - `.context/specs/sources/hygiene-map/hmap-p002.md`
- Load the map of interactional hygiene p003:
  - `.context/specs/sources/hygiene-map/hmap-p003.md`
- Load the map of interactional hygiene p004:
  - `.context/specs/sources/hygiene-map/hmap-p004.md`
- Load the map of interactional hygiene p005:
  - `.context/specs/sources/hygiene-map/hmap-p005.md`
- Load the map of interactional hygiene p006:
  - `.context/specs/sources/hygiene-map/hmap-p006.md`
- Load the map of interactional hygiene p007:
  - `.context/specs/sources/hygiene-map/hmap-p007.md`
- Load the map of interactional hygiene p008:
  - `.context/specs/sources/hygiene-map/hmap-p008.md`
- Load the map of interactional hygiene p009:
  - `.context/specs/sources/hygiene-map/hmap-p009.md`
- Load the map of interactional hygiene p010:
  - `.context/specs/sources/hygiene-map/hmap-p010.md`
- Load the map of interactional hygiene p011:
  - `.context/specs/sources/hygiene-map/hmap-p011.md`
- Load the map of interactional hygiene p012:
  - `.context/specs/sources/hygiene-map/hmap-p012.md`
- Load the map of interactional hygiene p013:
  - `.context/specs/sources/hygiene-map/hmap-p013.md`
- Load the map of interactional hygiene p014:
  - `.context/specs/sources/hygiene-map/hmap-p014.md`
- Load the map of interactional hygiene p015:
  - `.context/specs/sources/hygiene-map/hmap-p015.md`
- Load the map of interactional hygiene p016:
  - `.context/specs/sources/hygiene-map/hmap-p016.md`
- Load the map of interactional hygiene p017:
  - `.context/specs/sources/hygiene-map/hmap-p017.md`
- Load the map of interactional hygiene p018:
  - `.context/specs/sources/hygiene-map/hmap-p018.md`
- Load the map of interactional hygiene p019:
  - `.context/specs/sources/hygiene-map/hmap-p019.md`
- Load the map of interactional hygiene p020:
  - `.context/specs/sources/hygiene-map/hmap-p020.md`
- Load the map of interactional hygiene p021:
  - `.context/specs/sources/hygiene-map/hmap-p021.md`
- Load the current spiral tag map:
  - `.context/TAG_INDEX_SPIRAL.md`
- Load any active project state (optional, if present):
  - `.context/project_state.md`

// turbo

- [ ] Load `.framework/v8.2-stable/modules/Core_Identity.md` — current framework identity
- [ ] Load `.framework/v8.2-stable/modules/Athena_Xero_Orientation.md` — xero: framework orientation 
- [ ] Load `.context/TAG_INDEX.md`
- [ ] Load `.context/TAG_INDEX_SPIRAL.md`  
- [ ] Load `.context/project_state.md` — Current workspace state
- [ ] Create new session log in `session_logs/`

**Confirm**: "⚡ Ready. (Core Identity loaded. Session XX started.)"

## Phase 2: Triple-Lock Reminder

Every response MUST follow the Triple-Lock:

1. **Search** (Semantic) → FIRST
2. **Save** (Quicksave) → SECOND
3. **Speak** (Response) → LAST

---

## Quick Reference

| Command | Effect |
|---------|--------|
| `/start` | Boot system (this workflow) |
| `/end` | Close session, commit to memory |
| `/think` | Deep reasoning mode |
| `/ultrathink` | Maximum depth analysis |

---

# workflow #boot #start
